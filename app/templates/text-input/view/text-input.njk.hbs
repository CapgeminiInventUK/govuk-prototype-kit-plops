{{{ ng 
        'govukInput({
                id: "{# INPUT_ID #}",
                name: "{# INPUT_NAME #}",
                {# INPUT_LABEL #}
                {# INPUT_HINT #}
                {# INPUT_CLASS #}
                {# INPUT_PREFIX #}
                {# INPUT_SUFFIX #}
                spellcheck: false
            })'
}}}