{{{ ng 

    'govukRadios({
        id: "{# INPUT_ID #}",
        name: "{# INPUT_NAME #}",
        fieldset: {
            legend: {# INPUT_LEGEND #}
        },
        {# INPUT_HINT #}
        items: {# INPUT_ITEMS #}
    })'

}}}
